#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../yuglify/bin/yuglify" "$@"
  ret=$?
else 
  node  "$basedir/../yuglify/bin/yuglify" "$@"
  ret=$?
fi
exit $ret
